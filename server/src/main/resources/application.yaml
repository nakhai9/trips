spring:
  application:
    name: server

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-2290009c-hadt.i.aivencloud.com:17758/defaultdb?sslMode=REQUIRED
    username: avnadmin
    password: AVNS_q7JGdLtW0qVI0Rdlte4

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true

  devtools:
    restart:
      enabled: true
      poll-interval: 2s
      quiet-period: 1s

server:
  port: ${PORT:8080}

logging:
  level:
    org.hibernate.orm.jdbc.bind: trace
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

app:
  cors:
    allowed-origins: "*"